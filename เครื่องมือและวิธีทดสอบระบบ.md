# 🛠️ เครื่องมือและวิธีทดสอบระบบ

> สำหรับโปรเจค OOP Health Application - คู่มือทดสอบแบบรวดเร็ว

---

## 📋 สารบัญ

1. [เครื่องมือที่แนะนำ](#เครื่องมือที่แนะนำ)
2. [การทดสอบ Backend API](#การทดสอบ-backend-api)
3. [การทดสอบ Frontend](#การทดสอบ-frontend)
4. [Checklist การทดสอบอย่างรวดเร็ว](#checklist-การทดสอบอย่างรวดเร็ว)
5. [Script สำหรับทดสอบอัตโนมัติ](#script-สำหรับทดสอบอัตโนมัติ)

---

## 🛠️ เครื่องมือที่แนะนำ

### 1. **Postman** (แนะนำมากที่สุด ⭐⭐⭐⭐⭐)
**เหมาะสำหรับ:** ทดสอบ API ทั้งหมด

#### ดาวน์โหลดและติดตั้ง:
- **เว็บไซต์:** https://www.postman.com/downloads/
- **แบบ Desktop App:** Free (มี Sign up ฟรี)
- **แบบ Browser Extension:** มีแต่ไม่แนะนำมากเท่า

#### ข้อดี:
- ✅ ใช้งานง่าย สวยงาม
- ✅ สร้าง Collection เก็บ API ทั้งหมด
- ✅ Export/Import Collection ได้
- ✅ รองรับ Environment Variables
- ✅ บันทึก History
- ✅ สร้าง Test Scripts

---

### 2. **Insomnia** (ทางเลือกที่ดี ⭐⭐⭐⭐)
**เหมาะสำหรับ:** ทดสอบ API (เบากว่า Postman)

#### ดาวน์โหลดและติดตั้ง:
- **เว็บไซต์:** https://insomnia.rest/download
- **Free:** ใช่

#### ข้อดี:
- ✅ เร็ว เบา
- ✅ UI สะอาด
- ✅ ใช้งานง่าย

---

### 3. **Chrome DevTools** (มีอยู่แล้ว) ⭐⭐⭐⭐⭐
**เหมาะสำหรับ:** ทดสอบ Frontend, ดู Network, Debug JavaScript

#### วิธีเปิด:
- กด `F12` หรือ `Ctrl + Shift + I`
- คลิกขวา → "Inspect"

#### ฟีเจอร์ที่สำคัญ:
- **Console Tab:** ดู JavaScript errors
- **Network Tab:** ดู API calls
- **Application Tab:** ดู localStorage, sessionStorage
- **Elements Tab:** Inspect HTML/CSS

---

### 4. **cURL** (Command Line) ⭐⭐⭐⭐
**เหมาะสำหรับ:** ทดสอบ API แบบเร็ว, เขียน Script

#### Windows (PowerShell/CMD):
- มีอยู่แล้วใน Windows 10/11
- หรือติดตั้ง Git Bash

---

### 5. **Thunder Client** (VS Code Extension) ⭐⭐⭐⭐
**เหมาะสำหรับ:** ทดสอบ API ใน VS Code โดยตรง

#### ติดตั้ง:
1. เปิด VS Code
2. ไปที่ Extensions (Ctrl + Shift + X)
3. ค้นหา "Thunder Client"
4. Install

#### ข้อดี:
- ✅ ไม่ต้องสลับโปรแกรม
- ✅ ใช้งานใน VS Code โดยตรง

---

## 🔌 การทดสอบ Backend API

### ขั้นตอนที่ 1: รัน Backend Server

```bash
cd oop-backend
.\mvnw.cmd spring-boot:run
```

**ตรวจสอบว่า Server รันแล้ว:**
- เปิด Browser ไปที่: http://localhost:8080/actuator/health
- ควรเห็น: `{"status":"UP"}`

---

### ขั้นตอนที่ 2: ทดสอบด้วย Postman

#### สร้าง Postman Collection

1. **เปิด Postman**
2. **สร้าง Collection ใหม่:**
   - คลิก "New" → "Collection"
   - ตั้งชื่อ: "OOP Health App APIs"
   - Base URL: `http://localhost:8080`

3. **เพิ่ม Environment Variables:**
   - คลิก "Environments" → "Create Environment"
   - ตั้งชื่อ: "Local Development"
   - เพิ่ม Variables:
     - `base_url`: `http://localhost:8080`
     - `username`: `testuser`
     - `password`: `testpass`

---

#### ทดสอบ Authentication APIs

##### 1. Register (สมัครสมาชิก)

```
Method: POST
URL: {{base_url}}/register
Headers:
  Content-Type: application/json
Body (raw JSON):
{
  "username": "newuser123",
  "password": "newpass123"
}
```

**Expected Response (200 OK):**
```json
{
  "status": "success",
  "message": "User registered successfully",
  "username": "newuser123",
  "role": "Member"
}
```

**ทดสอบ Username ซ้ำ:**
```json
{
  "username": "newuser123",
  "password": "password"
}
```
**Expected:** `{"status": "failed", "message": "Username already taken"}`

---

##### 2. Login (เข้าสู่ระบบ)

```
Method: POST
URL: {{base_url}}/login
Headers:
  Content-Type: application/json
Body (raw JSON):
{
  "username": "newuser123",
  "password": "newpass123"
}
```

**Expected Response (200 OK):**
```json
{
  "status": "success",
  "role": "Member",
  "username": "newuser123"
}
```

**ทดสอบ Login ผิด:**
```json
{
  "username": "wronguser",
  "password": "wrongpass"
}
```
**Expected:** `{"status": "failed", "message": "Invalid username or password"}`

---

#### ทดสอบ Food APIs

##### 3. Get All Foods

```
Method: GET
URL: {{base_url}}/api/foods
```

**Expected Response (200 OK):**
```json
[
  {
    "name": "ข้าวผัด",
    "kcal": 350,
    "fat": 12,
    "sugar": 5,
    "sodium": 800
  },
  ...
]
```

---

##### 4. Search Food by Name

```
Method: GET
URL: {{base_url}}/api/foods/ข้าวผัด
```

**Expected Response (200 OK):**
```json
{
  "name": "ข้าวผัด",
  "kcal": 350,
  "fat": 12,
  "sugar": 5,
  "sodium": 800
}
```

---

#### ทดสอบ BMI APIs

##### 5. Calculate BMI

```
Method: POST
URL: {{base_url}}/api/bmi/calculate
Headers:
  Content-Type: application/json
Body (raw JSON):
{
  "username": "newuser123",
  "weight": 70,
  "height": 175
}
```

**Expected Response (200 OK):**
```json
{
  "bmi": 22.86,
  "category": "ปกติ",
  "recommendedCalories": 2100
}
```

---

##### 6. Get BMI Data

```
Method: GET
URL: {{base_url}}/api/bmi/newuser123
```

**Expected Response (200 OK):**
```json
{
  "username": "newuser123",
  "weight": 70,
  "height": 175,
  "bmi": 22.86,
  "category": "ปกติ",
  "recommendedCalories": 2100
}
```

---

#### ทดสอบ Cart APIs

##### 7. Add to Cart

```
Method: POST
URL: {{base_url}}/api/cart/add
Headers:
  Content-Type: application/json
Body (raw JSON):
{
  "username": "newuser123",
  "foodName": "ข้าวผัด"
}
```

**Expected Response (200 OK):**
```json
{
  "status": "success",
  "message": "Food added to cart"
}
```

---

##### 8. Get Cart

```
Method: GET
URL: {{base_url}}/api/cart/newuser123
```

**Expected Response (200 OK):**
```json
[
  {
    "name": "ข้าวผัด",
    "kcal": 350,
    "fat": 12,
    "sugar": 5,
    "sodium": 800
  }
]
```

---

##### 9. Remove from Cart

```
Method: DELETE
URL: {{base_url}}/api/cart/remove
Headers:
  Content-Type: application/json
Body (raw JSON):
{
  "username": "newuser123",
  "foodName": "ข้าวผัด"
}
```

**Expected Response (200 OK):**
```json
{
  "status": "success",
  "message": "Food removed from cart"
}
```

---

#### ทดสอบ Food Tracking APIs

##### 10. Add Tracking

```
Method: POST
URL: {{base_url}}/api/tracking/add
Headers:
  Content-Type: application/json
Body (raw JSON):
{
  "username": "newuser123",
  "foodName": "ข้าวผัด"
}
```

**Expected Response (200 OK):**
```json
{
  "status": "success",
  "message": "บันทึกการทานอาหารเรียบร้อย"
}
```

---

##### 11. Get Today Tracking

```
Method: GET
URL: {{base_url}}/api/tracking/today/newuser123
```

**Expected Response (200 OK):**
```json
[
  {
    "foodName": "ข้าวผัด",
    "kcal": 350,
    "fat": 12,
    "sugar": 5,
    "sodium": 800,
    "timestamp": "2024-01-15T10:30:00",
    "dateKey": "2024-01-15"
  }
]
```

---

##### 12. Get All Tracking

```
Method: GET
URL: {{base_url}}/api/tracking/all/newuser123
```

---

##### 13. Get Tracking by Date Range

```
Method: GET
URL: {{base_url}}/api/tracking/range/newuser123?startDate=2024-01-01&endDate=2024-01-31
```

---

##### 14. Delete Today Tracking

```
Method: DELETE
URL: {{base_url}}/api/tracking/today/newuser123
```

**Expected Response (200 OK):**
```json
{
  "status": "success",
  "message": "ลบประวัติการทานอาหารวันนี้เรียบร้อย"
}
```

---

### ขั้นตอนที่ 3: ทดสอบด้วย cURL (สำหรับคนที่ชอบ Command Line)

#### ตัวอย่าง cURL Commands

##### Register
```bash
curl -X POST http://localhost:8080/register ^
  -H "Content-Type: application/json" ^
  -d "{\"username\":\"testuser\",\"password\":\"testpass\"}"
```

##### Login
```bash
curl -X POST http://localhost:8080/login ^
  -H "Content-Type: application/json" ^
  -d "{\"username\":\"testuser\",\"password\":\"testpass\"}"
```

##### Get All Foods
```bash
curl http://localhost:8080/api/foods
```

##### Calculate BMI
```bash
curl -X POST http://localhost:8080/api/bmi/calculate ^
  -H "Content-Type: application/json" ^
  -d "{\"username\":\"testuser\",\"weight\":70,\"height\":175}"
```

---

## 🌐 การทดสอบ Frontend

### ขั้นตอนที่ 1: รัน Frontend Server

```bash
cd oop-frontend
python -m http.server 8000
```

เปิด Browser: http://localhost:8000/login.html

---

### ขั้นตอนที่ 2: เปิด Chrome DevTools

1. **กด F12** หรือ `Ctrl + Shift + I`
2. **ไปที่ Console Tab:**
   - ดู JavaScript errors (สีแดง)
   - ดู console.log messages

3. **ไปที่ Network Tab:**
   - ดู API calls ทั้งหมด
   - ตรวจสอบ Status Code (ควรเป็น 200)
   - ดู Request/Response

4. **ไปที่ Application Tab:**
   - Local Storage → ดู `currentUser`
   - Session Storage

---

### ขั้นตอนที่ 3: Manual Testing Checklist

#### ✅ ทดสอบ Login
- [ ] กรอก username/password → Login สำเร็จ
- [ ] Username/password ผิด → แสดง error
- [ ] ช่องว่าง → แสดง validation
- [ ] Password visibility toggle ทำงาน

#### ✅ ทดสอบ Register
- [ ] สมัครสำเร็จ
- [ ] Username ซ้ำ → แสดง error
- [ ] Password ไม่ตรงกัน → แสดง error
- [ ] Password visibility toggle ทำงาน

#### ✅ ทดสอบ Home Page
- [ ] แสดงรายการอาหาร
- [ ] ค้นหาอาหาร
- [ ] เรียงลำดับ (แคลอรี่, ไขมัน, น้ำตาล, โซเดียม)
- [ ] กรองตามอารมณ์
- [ ] คลิกดูรายละเอียดอาหาร
- [ ] ข่าวสุขภาพเลื่อนอัตโนมัติ
- [ ] เพิ่มอาหารลง Tracking
- [ ] ดู Tracking Summary

#### ✅ ทดสอบ Profile
- [ ] คำนวณ BMI
- [ ] บันทึกข้อมูล BMI
- [ ] แสดงข้อมูล BMI ที่บันทึกไว้

#### ✅ ทดสอบ Cart
- [ ] เพิ่มอาหารลงตะกร้า
- [ ] ดูตะกร้า
- [ ] ลบรายการ
- [ ] ล้างตะกร้า

#### ✅ ทดสอบ Food Battle
- [ ] เลือกอาหาร 2 รายการ
- [ ] ดูผลการเปรียบเทียบ
- [ ] เปลี่ยนอาหารที่เลือก

---

## ✅ Checklist การทดสอบอย่างรวดเร็ว (Quick Test)

### ⚡ Quick Smoke Test (5 นาที)

1. **รัน Backend:** `cd oop-backend && .\mvnw.cmd spring-boot:run`
2. **รัน Frontend:** `cd oop-frontend && python -m http.server 8000`
3. **ทดสอบ Login:**
   - ใช้ user ที่มีอยู่: `member` / `123`
   - ตรวจสอบว่า login สำเร็จ
4. **ทดสอบ API หลัก:**
   - GET `/api/foods` → ควรได้รายการอาหาร
5. **ทดสอบ Frontend หลัก:**
   - เปิด http://localhost:8000/login.html
   - Login สำเร็จ → ไปหน้า home
   - ดูรายการอาหารแสดง
   - คลิกดูรายละเอียดอาหาร

**ถ้าทุกอย่างทำงาน = ระบบพื้นฐานพร้อม ✅**

---

### 📋 Full Functional Test (30-60 นาที)

ทำตาม **แผนการทดสอบระบบ.md** ที่สร้างไว้แล้ว

---

## 📜 Script สำหรับทดสอบอัตโนมัติ

### PowerShell Script สำหรับทดสอบ Backend

สร้างไฟล์ `test-apis.ps1`:

```powershell
# Test Backend APIs Script
$baseUrl = "http://localhost:8080"

Write-Host "=== Testing Backend APIs ===" -ForegroundColor Green

# Test 1: Register
Write-Host "`n1. Testing Register..." -ForegroundColor Yellow
$registerBody = @{
    username = "testuser$(Get-Random)"
    password = "testpass123"
} | ConvertTo-Json

$registerResponse = Invoke-RestMethod -Uri "$baseUrl/register" -Method Post -Body $registerBody -ContentType "application/json"
Write-Host "Register Result: $($registerResponse.status)" -ForegroundColor Cyan

# Test 2: Login
Write-Host "`n2. Testing Login..." -ForegroundColor Yellow
$loginBody = @{
    username = "member"
    password = "123"
} | ConvertTo-Json

$loginResponse = Invoke-RestMethod -Uri "$baseUrl/login" -Method Post -Body $loginBody -ContentType "application/json"
Write-Host "Login Result: $($loginResponse.status)" -ForegroundColor Cyan

# Test 3: Get All Foods
Write-Host "`n3. Testing Get All Foods..." -ForegroundColor Yellow
$foods = Invoke-RestMethod -Uri "$baseUrl/api/foods" -Method Get
Write-Host "Found $($foods.Count) foods" -ForegroundColor Cyan

# Test 4: Calculate BMI
Write-Host "`n4. Testing BMI Calculate..." -ForegroundColor Yellow
$bmiBody = @{
    username = "member"
    weight = 70
    height = 175
} | ConvertTo-Json

$bmiResponse = Invoke-RestMethod -Uri "$baseUrl/api/bmi/calculate" -Method Post -Body $bmiBody -ContentType "application/json"
Write-Host "BMI: $($bmiResponse.bmi), Category: $($bmiResponse.category)" -ForegroundColor Cyan

Write-Host "`n=== Test Complete ===" -ForegroundColor Green
```

**วิธีใช้:**
```powershell
.\test-apis.ps1
```

---

### Batch Script สำหรับรัน Backend และ Frontend

สร้างไฟล์ `start-dev.bat`:

```batch
@echo off
echo Starting Development Servers...

echo.
echo Starting Backend Server...
start "Backend Server" cmd /k "cd oop-backend && .\mvnw.cmd spring-boot:run"

timeout /t 10 /nobreak > nul

echo.
echo Starting Frontend Server...
start "Frontend Server" cmd /k "cd oop-frontend && python -m http.server 8000"

timeout /t 3 /nobreak > nul

echo.
echo Opening Browser...
start http://localhost:8000/login.html

echo.
echo Servers are running!
echo Backend: http://localhost:8080
echo Frontend: http://localhost:8000
pause
```

**วิธีใช้:**
- ดับเบิลคลิก `start-dev.bat`
- จะเปิด Backend, Frontend และ Browser อัตโนมัติ

---

## 🔍 การ Debug และ Troubleshooting

### ปัญหาที่พบบ่อย

#### 1. Backend ไม่ทำงาน
**อาการ:** API calls ไม่สำเร็จ, 500 Error

**แก้ไข:**
```bash
# ตรวจสอบว่า Server รันอยู่หรือไม่
curl http://localhost:8080/actuator/health

# ดู logs ใน Console
# ตรวจสอบว่า Port 8080 ไม่ถูกใช้งาน
netstat -ano | findstr :8080
```

---

#### 2. CORS Error
**อาการ:** Console แสดง "CORS policy" error

**แก้ไข:**
- ตรวจสอบว่า Backend มี `@CrossOrigin(origins = "*")` ใน Controller

---

#### 3. Frontend ไม่แสดงข้อมูล
**อาการ:** หน้าเว็บว่าง, ไม่มีข้อมูล

**แก้ไข:**
- เปิด DevTools (F12) → Console Tab
- ดู JavaScript errors
- ดู Network Tab → ตรวจสอบ API calls

---

#### 4. localStorage ไม่ทำงาน
**อาการ:** Login แล้วแต่กลับไปหน้า login อีก

**แก้ไข:**
- เปิด DevTools (F12) → Application Tab → Local Storage
- ตรวจสอบว่ามี `currentUser` หรือไม่
- ลอง Clear Storage และ Login ใหม่

---

## 📊 สรุปเครื่องมือที่แนะนำตามความต้องการ

| ความต้องการ | เครื่องมือที่แนะนำ | เหตุผล |
|------------|------------------|--------|
| **ทดสอบ API อย่างละเอียด** | Postman | ครบถ้วน, ง่ายต่อการจัดการ |
| **ทดสอบ API แบบเร็ว** | cURL หรือ Thunder Client | รวดเร็ว, ไม่ต้องเปิดโปรแกรมเพิ่ม |
| **Debug Frontend** | Chrome DevTools | มีอยู่แล้ว, ครบถ้วน |
| **ทดสอบทั้งระบบ** | Manual Testing + Checklist | ครอบคลุมทุกฟีเจอร์ |

---

## 🎯 แผนการทดสอบสำหรับพรุ่งนี้

### ช่วงเช้า (30 นาที)
1. ✅ รัน Backend และ Frontend
2. ✅ Quick Smoke Test (5 นาที)
3. ✅ ทดสอบ API หลักๆ ด้วย Postman (15 นาที)
4. ✅ ทดสอบ Frontend หลักๆ (10 นาที)

### ช่วงบ่าย (60-90 นาที)
1. ✅ Full Functional Test ตาม Checklist
2. ✅ ทดสอบ Edge Cases
3. ✅ ทดสอบ Error Handling
4. ✅ ทดสอบ Responsive Design (Mobile/Tablet/Desktop)

### ก่อนส่งงาน (15 นาที)
1. ✅ Final Check: ทุกฟีเจอร์ทำงาน
2. ✅ ตรวจสอบไม่มี Console Errors
3. ✅ ตรวจสอบไม่มี Network Errors
4. ✅ จัดทำ Test Report

---

## 💡 เคล็ดลับสำหรับการทดสอบ

1. **ใช้ Browser Incognito Mode** - เพื่อหลีกเลี่ยง Cache
2. **Clear LocalStorage** - ก่อนทดสอบ Login/Register ใหม่
3. **บันทึก Screen Recording** - ถ้าเจอ Bug เพื่อแสดงให้อาจารย์ดู
4. **ใช้ Postman Collection** - เพื่อทดสอบ API อย่างเป็นระบบ
5. **ทดสอบในหลาย Browser** - Chrome, Firefox, Edge

---

**Good Luck with Testing! 🚀**

*อัปเดตล่าสุด: วันนี้*

