# 📚 คู่มือการ Setup และใช้งาน Spring Boot Backend

คู่มือนี้จะช่วยให้คุณสามารถติดตั้งและรัน Spring Boot Backend บนคอมพิวเตอร์เครื่องใหม่ได้อย่างง่ายดาย

---

## 📋 สารบัญ

1. [ข้อกำหนดเบื้องต้น](#ข้อกำหนดเบื้องต้น)
2. [ขั้นตอนการติดตั้ง](#ขั้นตอนการติดตั้ง)
3. [วิธีรันโปรเจค](#วิธีรันโปรเจค)
4. [วิธีทดสอบ API](#วิธีทดสอบ-api)
5. [โครงสร้างโปรเจค](#โครงสร้างโปรเจค)
6. [แก้ไขปัญหา (Troubleshooting)](#แก้ไขปัญหา-troubleshooting)
7. [คำถามที่พบบ่อย (FAQ)](#คำถามที่พบบ่อย-faq)

---

## 🔧 ข้อกำหนดเบื้องต้น

ก่อนเริ่มต้น คุณต้องมีโปรแกรมเหล่านี้ติดตั้งอยู่บนเครื่อง:

### 1. Java Development Kit (JDK) 17 หรือสูงกว่า

**ตรวจสอบว่ามี Java ติดตั้งอยู่หรือไม่:**
```bash
java -version
```

**ถ้ายังไม่มี Java:**
- **Windows:** ดาวน์โหลดจาก [Oracle JDK](https://www.oracle.com/java/technologies/downloads/#java17) หรือ [OpenJDK](https://adoptium.net/)
- **macOS:** ใช้ Homebrew: `brew install openjdk@17`
- **Linux:** `sudo apt-get install openjdk-17-jdk` (Ubuntu/Debian)

**ตั้งค่า JAVA_HOME:**
- **Windows:** เพิ่ม Environment Variable `JAVA_HOME` ชี้ไปที่โฟลเดอร์ JDK
- **macOS/Linux:** เพิ่มใน `~/.bashrc` หรือ `~/.zshrc`:
  ```bash
  export JAVA_HOME=/path/to/jdk-17
  export PATH=$JAVA_HOME/bin:$PATH
  ```

### 2. Maven (ไม่จำเป็น - โปรเจคมี Maven Wrapper อยู่แล้ว)

โปรเจคนี้มี Maven Wrapper (`mvnw` และ `mvnw.cmd`) อยู่แล้ว ดังนั้นคุณไม่จำเป็นต้องติดตั้ง Maven แยก

**แต่ถ้าต้องการติดตั้ง Maven:**
- **Windows:** ดาวน์โหลดจาก [Maven Website](https://maven.apache.org/download.cgi)
- **macOS:** `brew install maven`
- **Linux:** `sudo apt-get install maven`

---

## 🚀 ขั้นตอนการติดตั้ง

### ขั้นตอนที่ 1: ดาวน์โหลดโปรเจค

1. คัดลอกโฟลเดอร์ `oop-backend` ไปยังคอมพิวเตอร์เครื่องใหม่
2. เปิด Terminal หรือ Command Prompt
3. ไปยังโฟลเดอร์โปรเจค:
   ```bash
   cd oop-backend
   ```

### ขั้นตอนที่ 2: ตรวจสอบไฟล์สำคัญ

ตรวจสอบว่ามีไฟล์เหล่านี้อยู่ในโฟลเดอร์:
- ✅ `pom.xml` - ไฟล์กำหนด dependencies
- ✅ `mvnw` (Linux/macOS) หรือ `mvnw.cmd` (Windows) - Maven Wrapper
- ✅ `src/main/java/com/login_oop/oop_backend/OopBackendApplication.java` - Main class

### ขั้นตอนที่ 3: (ถ้าจำเป็น) ให้สิทธิ์ execute กับ Maven Wrapper

**สำหรับ Linux/macOS:**
```bash
chmod +x mvnw
```

**สำหรับ Windows:** ไม่ต้องทำอะไร (ใช้ `mvnw.cmd` ได้เลย)

---

## ▶️ วิธีรันโปรเจค

### วิธีที่ 1: ใช้ Maven Wrapper (แนะนำ)

**Windows:**
```bash
.\mvnw.cmd spring-boot:run
```

**Linux/macOS:**
```bash
./mvnw spring-boot:run
```

### วิธีที่ 2: ใช้ Maven (ถ้าติดตั้งแล้ว)

```bash
mvn spring-boot:run
```

### วิธีที่ 3: Build แล้วรัน JAR file

**Build:**
```bash
# Windows
.\mvnw.cmd clean package

# Linux/macOS
./mvnw clean package
```

**รัน:**
```bash
java -jar target/oop-backend-0.0.1-SNAPSHOT.jar
```

### วิธีที่ 4: ใช้ IDE (IntelliJ IDEA / Eclipse / VS Code)

1. เปิด IDE
2. เปิดโปรเจค (File → Open → เลือกโฟลเดอร์ `oop-backend`)
3. รอให้ IDE โหลด dependencies (อาจใช้เวลาสักครู่)
4. คลิกขวาที่ไฟล์ `OopBackendApplication.java`
5. เลือก "Run" หรือ "Debug"

---

## ✅ ตรวจสอบว่า Backend ทำงาน

เมื่อรันสำเร็จ คุณจะเห็นข้อความประมาณนี้:

```
  .   ____          _            __ _ _
 /\\ / ___'_ __ _ _(_)_ __  __ _ \ \ \ \
( ( )\___ | '_ | '_| | '_ \/ _` | \ \ \ \
 \\/  ___)| |_)| | | | | || (_| |  ) ) ) )
  '  |____| .__|_| |_|_| |_\__, | / / / /
 =========|_|==============|___/=/_/_/_/
 :: Spring Boot ::                (v3.5.7)

2024-XX-XX ... Started OopBackendApplication in X.XXX seconds
```

**ทดสอบว่า Server ทำงาน:**
- เปิดเบราว์เซอร์ไปที่: `http://localhost:8080`
- หรือทดสอบ API: `http://localhost:8080/api/foods`

---

## 🧪 วิธีทดสอบ API

### 1. ใช้ Browser

เปิดเบราว์เซอร์และไปที่:
- `http://localhost:8080/api/foods` - ดูรายการอาหารทั้งหมด

### 2. ใช้ Postman หรือ Insomnia

**ตัวอย่าง API Endpoints:**

**Login:**
```
POST http://localhost:8080/login
Content-Type: application/json

{
  "username": "testuser",
  "password": "testpass"
}
```

**ดึงรายการอาหาร:**
```
GET http://localhost:8080/api/foods
```

**คำนวณ BMI:**
```
POST http://localhost:8080/api/bmi/calculate
Content-Type: application/json

{
  "username": "testuser",
  "weight": 70,
  "height": 175
}
```

### 3. ใช้ cURL (Command Line)

**ทดสอบ Login:**
```bash
curl -X POST http://localhost:8080/login \
  -H "Content-Type: application/json" \
  -d "{\"username\":\"testuser\",\"password\":\"testpass\"}"
```

**ดึงรายการอาหาร:**
```bash
curl http://localhost:8080/api/foods
```

---

## 📁 โครงสร้างโปรเจค

```
oop-backend/
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── com/login_oop/oop_backend/
│   │   │       ├── OopBackendApplication.java    # Main class
│   │   │       ├── controllers/                  # REST Controllers
│   │   │       │   ├── AuthController.java       # Login/Register
│   │   │       │   ├── BMIController.java        # BMI Calculation
│   │   │       │   ├── CartController.java       # Shopping Cart
│   │   │       │   ├── FoodController.java        # Food List
│   │   │       │   └── FoodTrackingController.java # Food Tracking
│   │   │       ├── services/                     # Business Logic
│   │   │       ├── repositories/                 # Data Access
│   │   │       ├── models/                       # Data Models
│   │   │       └── dto/                          # Data Transfer Objects
│   │   └── resources/
│   │       ├── application.properties            # Configuration
│   │       └── users.txt                         # User Data File
│   └── test/                                     # Test Files
├── pom.xml                                       # Maven Dependencies
├── mvnw                                          # Maven Wrapper (Linux/macOS)
├── mvnw.cmd                                      # Maven Wrapper (Windows)
└── README.md                                     # คู่มือนี้
```

---

## 🔧 แก้ไขปัญหา (Troubleshooting)

### ❌ ปัญหา: "java: command not found" หรือ "ไม่พบคำสั่ง java"

**สาเหตุ:** Java ยังไม่ได้ติดตั้งหรือไม่ได้ตั้งค่า PATH

**วิธีแก้:**
1. ตรวจสอบว่า Java ติดตั้งแล้ว: `java -version`
2. ถ้ายังไม่มี ให้ติดตั้ง JDK 17
3. ตั้งค่า `JAVA_HOME` environment variable

---

### ❌ ปัญหา: "Port 8080 is already in use"

**สาเหตุ:** มีโปรแกรมอื่นใช้ port 8080 อยู่

**วิธีแก้:**

**Windows:**
```bash
# หา process ที่ใช้ port 8080
netstat -ano | findstr :8080

# Kill process (แทน PID ด้วยหมายเลขที่เจอ)
taskkill /PID <PID> /F
```

**Linux/macOS:**
```bash
# หา process ที่ใช้ port 8080
lsof -i :8080

# Kill process
kill -9 <PID>
```

**หรือเปลี่ยน port ใน `application.properties`:**
```properties
server.port=8081
```

---

### ❌ ปัญหา: "Maven dependencies download failed"

**สาเหตุ:** ไม่มีอินเทอร์เน็ตหรือ Maven repository ไม่สามารถเข้าถึงได้

**วิธีแก้:**
1. ตรวจสอบการเชื่อมต่ออินเทอร์เน็ต
2. ลองรันอีกครั้ง (อาจเป็นปัญหาชั่วคราว)
3. ถ้าใช้ proxy ให้ตั้งค่าใน `~/.m2/settings.xml`

---

### ❌ ปัญหา: "ClassNotFoundException" หรือ "NoClassDefFoundError"

**สาเหตุ:** Dependencies ยังไม่ได้โหลดหรือ build ไม่สำเร็จ

**วิธีแก้:**
```bash
# Clean และ rebuild
./mvnw clean install

# หรือ
./mvnw clean package
```

---

### ❌ ปัญหา: Frontend ไม่สามารถเชื่อมต่อ Backend ได้

**สาเหตุ:** CORS หรือ Backend ไม่ได้รัน

**วิธีแก้:**
1. ตรวจสอบว่า Backend กำลังรันอยู่ที่ `http://localhost:8080`
2. ตรวจสอบ Console ใน Browser (F12) ดู error message
3. ตรวจสอบว่า Controller มี `@CrossOrigin(origins = "*")` อยู่

---

## ❓ คำถามที่พบบ่อย (FAQ)

### Q: ต้องใช้ Java version อะไร?

**A:** Java 17 หรือสูงกว่า (โปรเจคนี้กำหนดไว้ใน `pom.xml`)

---

### Q: ต้องติดตั้ง Maven แยกไหม?

**A:** ไม่จำเป็น โปรเจคมี Maven Wrapper (`mvnw`) อยู่แล้ว

---

### Q: เปลี่ยน port ได้ไหม?

**A:** ได้ แก้ไขไฟล์ `src/main/resources/application.properties`:
```properties
server.port=8081
```

---

### Q: ข้อมูล user เก็บอยู่ที่ไหน?

**A:** เก็บในไฟล์ `src/main/resources/users.txt` (ใน memory ระหว่างรัน)

---

### Q: ต้องใช้ Database ไหม?

**A:** ไม่ต้อง โปรเจคนี้ใช้ In-Memory Storage (เก็บข้อมูลใน memory ระหว่างรัน)

---

### Q: วิธีรันแบบ Production?

**A:** Build JAR file แล้วรัน:
```bash
./mvnw clean package
java -jar target/oop-backend-0.0.1-SNAPSHOT.jar
```

---

### Q: วิธี Debug?

**A:** 
- **IDE:** ตั้ง breakpoint แล้วกด Debug
- **Command Line:** เพิ่ม `-Xdebug` และ `-Xrunjdwp` options

---

## 📝 หมายเหตุสำคัญ

1. **ข้อมูลจะหายเมื่อปิด Server:** โปรเจคนี้ใช้ In-Memory Storage ข้อมูลจะหายเมื่อปิด server
2. **Port 8080:** ตรวจสอบว่าไม่มีโปรแกรมอื่นใช้ port นี้อยู่
3. **CORS:** Backend อนุญาต CORS จากทุก origin (`@CrossOrigin(origins = "*")`)
4. **Development Mode:** Spring Boot DevTools จะ auto-reload เมื่อแก้ไขโค้ด

---

## 🎯 สรุปขั้นตอนเร็ว (Quick Start)

```bash
# 1. ไปยังโฟลเดอร์โปรเจค
cd oop-backend

# 2. รันโปรเจค
# Windows:
.\mvnw.cmd spring-boot:run

# Linux/macOS:
./mvnw spring-boot:run

# 3. เปิดเบราว์เซอร์ไปที่
http://localhost:8080/api/foods
```

---

## 📞 ขอความช่วยเหลือ

ถ้ายังมีปัญหา:
1. ตรวจสอบ Console logs ดู error message
2. ตรวจสอบว่า Java version ถูกต้อง: `java -version`
3. ตรวจสอบว่า port 8080 ว่างอยู่
4. ลอง clean และ rebuild: `./mvnw clean install`

---

**Happy Coding! 🚀**

