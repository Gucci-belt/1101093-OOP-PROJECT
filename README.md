# 🥗 OOP Project: ระบบสั่งอาหารเพื่อสุขภาพและคำนวณ BMI

โปรเจคนี้เป็นระบบสั่งอาหารออนไลน์ที่เน้นสุขภาพ พร้อมฟีเจอร์คำนวณ BMI และแนะนำอาหารตามอารมณ์ (Mood-based Dining) พัฒนาด้วย Java Spring Boot และ HTML/CSS/JS

## 📋 สิ่งที่ต้องมี (Prerequisites)
1.  **Java JDK 17** หรือใหม่กว่า
2.  **Web Browser** (Chrome, Edge, Firefox)
3.  **Internet Connection** (สำหรับโหลดรูปภาพและ Font)

---

## 🚀 วิธีรันโปรแกรม (Step-by-Step)

### ส่วนที่ 1: เปิด Backend (Server)
Backend จะทำหน้าที่จัดการข้อมูลและคำนวณต่างๆ ต้องเปิดทิ้งไว้ตลอดเวลาใช้งาน

**วิธีที่ 1: รันผ่าน Command Line (แนะนำ)**
1.  เปิด Terminal หรือ Command Prompt
2.  พิมพ์คำสั่งเพื่อเข้าไปที่โฟลเดอร์ backend:
    ```cmd
    cd oop-backend
    ```
3.  รันคำสั่งเพื่อเริ่ม Server:
    *   **Windows:**
        ```cmd
        .\mvnw.cmd spring-boot:run
        ```
    *   **Mac/Linux:**
        ```bash
        ./mvnw spring-boot:run
        ```
4.  รอจนกว่าจะขึ้นข้อความว่า `Started OopBackendApplication in ... seconds`

**วิธีที่ 2: รันผ่าน IDE (VS Code / IntelliJ)**
1.  เปิดโฟลเดอร์ `oop-backend` ในโปรแกรม
2.  เปิดไฟล์ `src/main/java/com/login_oop/oop_backend/OopBackendApplication.java`
3.  กดปุ่ม **Run** หรือ **Debug**

---

### ส่วนที่ 2: เปิด Frontend (หน้าเว็บ)
1.  เข้าไปที่โฟลเดอร์ `oop-frontend`
2.  ดับเบิ้ลคลิกไฟล์ **`login.html`** เพื่อเปิดใน Browser
3.  (หรือคลิกขวา -> Open with -> Google Chrome)

---

## 📱 วิธีใช้งาน

### 1. การเข้าสู่ระบบ (Login)
*   **Username:** `admin` | **Password:** `admin` (สำหรับ Admin)
*   **Username:** `member` | **Password:** `123` (สำหรับสมาชิกทั่วไป)
*   หรือกดปุ่ม **"ลงทะเบียน"** เพื่อสมัครสมาชิกใหม่

### 2. หน้าหลัก (Home)
*   เลือกดูรายการอาหารพร้อมข้อมูลโภชนาการ
*   **✨ ฟีเจอร์ใหม่! Mood-based Dining:** กดปุ่มอารมณ์ด้านบน (เครียด, เหนื่อย, ลดหุ่น, บวมน้ำ) เพื่อให้อาหารแนะนำตามความต้องการ
*   กดปุ่ม `+` เพื่อเพิ่มอาหารลงตะกร้า

### 3. ตะกร้าสินค้า (Cart)
*   ดูรายการที่เลือก
*   กดลบรายการที่ไม่ต้องการ หรือกด "ชำระเงิน" (จำลอง)

### 4. แดชบอร์ดสุขภาพ (Health Dashboard)
*   กดไอคอน 📊 ที่เมนูด้านล่าง
*   ดูสถิติการกิน, คำนวณ BMI, และดูคำแนะนำสุขภาพ

---

## 🛠️ การแก้ไขปัญหาเบื้องต้น (Troubleshooting)

*   **Q: กด Login แล้วเงียบ / ไม่โหลดรายการอาหาร?**
    *   A: เช็คว่า Backend รันอยู่หรือไม่ (ดูหน้าจอ Terminal ว่ามี Error หรือเปล่า)
    *   A: เช็คว่า Port 8080 ว่างอยู่หรือไม่

*   **Q: รูปภาพไม่ขึ้น?**
    *   A: ตรวจสอบการเชื่อมต่ออินเทอร์เน็ต หรือเช็คชื่อไฟล์รูปในโฟลเดอร์ `images`

---

## 👨‍💻 ผู้จัดทำ
*   [ใส่ชื่อของคุณและรหัสนักศึกษาที่นี่]
*   วิชา Object-Oriented Programming
